# AVE

## Getting started

This project uses [Homebrew](https://brew.sh) to manage local development dependencies.

To bootstrap your environment:

```bash
chmod +x scripts/install-homebrew.sh
./scripts/install-homebrew.sh
```

This installs Homebrew if it isn't already present, then installs any packages listed in the `Brewfile`.
